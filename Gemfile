source 'http://rubygems.org'

gem 'minitest', :group => :test

# The core of ruby-redis is a redis 2.2 compatible server in Ruby.
# EventMachine is the only dependency.  Without EM we have no epoll.
gem 'eventmachine', '1.0.0.beta.3'
# gem 'eventmachine', :git => 'git://github.com/eventmachine/eventmachine.git'

# The goal of ruby-redis is a Ruby-scriptable REST interface
# gem 'rack'
# gem 'thin'
